---
import { siteConfig } from '../data/siteConfig';
import '../styles/global.css';

interface Props {
  title?: string;
  description?: string;
}

const { title = siteConfig.title, description = siteConfig.description } = Astro.props;

const structuredData = {
  "@context": "https://schema.org",
  "@graph": [
    {
      "@type": "Person",
      "@id": `${siteConfig.url}/#person`,
      "name": siteConfig.name,
      "jobTitle": siteConfig.subtitle,
      "description": siteConfig.description,
      "url": siteConfig.url,
      "image": {
        "@type": "ImageObject",
        "url": `${siteConfig.url}/images/avatar.png`,
        "width": 400,
        "height": 400
      },
      "sameAs": [
        "https://github.com/enjoydarts",
        "https://x.com/enjoydarts",
        "https://threads.net/darts",
        "https://enjoydarts.blog"
      ],
      "knowsAbout": [
        "Ruby",
        "PHP",
        "TypeScript",
        "Web Development",
        "Full Stack Development",
        "Ruby on Rails",
        "Laravel",
        "React",
        "Vue.js",
        "Next.js",
        "Tailwind CSS"
      ],
      "hasOccupation": {
        "@type": "Occupation",
        "name": "Freelance Web Engineer",
        "occupationLocation": {
          "@type": "Country",
          "name": "Japan"
        }
      }
    },
    {
      "@type": "WebSite",
      "@id": `${siteConfig.url}/#website`,
      "url": siteConfig.url,
      "name": siteConfig.title,
      "description": siteConfig.description,
      "author": {
        "@id": `${siteConfig.url}/#person`
      },
      "inLanguage": "ja-JP"
    }
  ]
};
---

<!doctype html>
<html lang="ja">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <meta name="description" content={description} />

    <!-- OGP -->
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="website" />
    <meta property="og:image" content={`${siteConfig.url}/og.png.png`} />

    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image" />
    <meta name="twitter:site" content="@enjoydarts" />
    <meta name="twitter:creator" content="@enjoydarts" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <meta name="twitter:image" content={`${siteConfig.url}/og.png.png`} />

    <!-- Favicons -->
    <link rel="icon" type="image/png" sizes="32x32" href="/images/favicon-32x32.png" />
    <link rel="icon" type="image/png" sizes="16x16" href="/images/favicon-16x16.png" />
    <link rel="apple-touch-icon" sizes="180x180" href="/images/apple-touch-icon.png" />

    <!-- Twitter Widgets -->
    <script async src="https://platform.twitter.com/widgets.js" charset="utf-8"></script>

    <!-- Structured Data (JSON-LD) -->
    <script type="application/ld+json" set:html={JSON.stringify(structuredData)} />
  </head>
  <body>
    <slot />
  </body>
</html>
